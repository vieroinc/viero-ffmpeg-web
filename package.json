{
  "name": "@viero/ffmpeg-web",
  "version": "1.0.0",
  "description": "Client side FFMpeg for web apps.",
  "homepage": "https://github.com/vieroinc/viero-ffmpeg-web",
  "contributors": [
    {
      "name": "Zoltan Gobolos",
      "email": "zoltan.gobolos@viero.tv"
    }
  ],
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/vieroinc/viero-ffmpeg-web.git"
  },
  "scripts": {
    "clean": "rimraf dist",
    "reset": "npm run clean && rimraf node_modules",
    "build": "npm run clean && babel src -d ./dist --source-maps inline",
    "build:wasm": "./scripts/build.sh",
    "dist": "npm run build && copyfiles package.json README.md LICENSE.md dist && copyfiles -u 1 src/worker/ffmpeg.js dist/",
    "lint": "eslint ./src",
    "npmjs": "npm publish ./dist --access public"
  },
  "module": "./index.js",
  "dependencies": {
    "@viero/common": "^1.5.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "copyfiles": "^2.3.0",
    "eslint": "^7.5.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0",
    "ncp": "^2.0.0",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2"
  }
}
